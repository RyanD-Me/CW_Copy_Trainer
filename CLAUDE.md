# CW Copy Trainer — Claude Code 引き継ぎメモ

アマチュア無線の CW（モールス電信）を**耳で聞き取る**練習用 Web アプリ。
ビルド不要の単一 HTML（`index.html`）で、ブラウザで開けばそのまま動く。UI はすべて日本語。

## ファイル構成

```
index.html            アプリ本体（HTML/CSS/JS すべてこの1ファイル。成果物はこれ）
data/                 出題データの元ファイル（index.html に埋め込み済み。実行時には読まない）
  jcc-list.txt        JARL 市番号リスト（Shift_JIS）
  jcg-list.txt        JARL 郡番号リスト（Shift_JIS）
  ku-list.txt         JARL 区番号リスト（Shift_JIS）
  shicho-list.txt     JARL 都府県・北海道地域番号表（参考。コードでは未使用）
  MASTER.txt          Super Check Partial（2026.09.23版）から J[A-S]/7[K-N] の6文字局だけ残したもの
  logs/               コンテスト公開ログ「コールサイン,ナンバー」（ユーザー提供。CRLF）
    allja-2025.txt allja-2026.txt 6d-2025.txt fd-2025.txt acag-2025.txt
tools/build_data.py   data/ から index.html 内の埋め込み配列を再生成（--check で差分確認）
tests/smoke.js        Playwright による簡易動作確認
```

## 作業ルール（ユーザー指定）

- **成果物のファイル名は `index.html`**。変更したら毎回ユーザーに最新の index.html を渡す。
- 外部ライブラリなし・ビルドなし・1ファイルを維持。フォントのみ Google Fonts（Noto Sans JP / Space Mono）。オフラインでも動くこと。
- 変更後の確認: `node --check`（script 部分）、`python3 tools/build_data.py --check`、`node tests/smoke.js`。
- **常にダークモード**（OS のライト/ダーク設定に関係なく）。配色は `:root` の CSS 変数1組だけ。ライト用の配色・切替は持たない。
- 記録・設定は localStorage のみ（`cwtrainer.settings` / `cwtrainer.stats` / `cwtrainer.highscores`）。すべて try/catch で包む。

## 音声・タイミング

- トーンは **760Hz 固定**（UI には表示しない）。正弦波、立ち上がり/立ち下がり 4ms。音量は masterGain で調整、ミュートも masterGain。
- 速度は「文字速度 Character Speed」と「**文字間の速度** Farnsworth」の2つ（単位 WPM、5〜60、数値入力＋▲▼ボタン）。文字間の速度は文字速度を超えない。
- Farnsworth 計算（`computeTiming`）: dit=1.2/Wc、追加間隔 tflc=(60/Wf−60/Wc)/19、文字間=3(dit+tflc)、単語間=7(dit+tflc)。
- 繰り返し再生（`playLoop`）の間隔、正解後に次の問題を鳴らすまでの間隔は「単語間×2」（最低0.7秒）。

## 画面とモード

### 通常練習（タブ「通常練習」）
- ▶再生/⏸一時停止は**1つのトグルボタン**（幅固定）。再生すると同じ問題を自動で繰り返す。**一時停止後の▶再生は続きから**（止めた符号の頭から。位置は区切りの番号 `practiceResume` で持つので、停止中に速度を変えてもずれない。`currentSegIndex` → `playLoop(text, 0, fromSeg)`）。「↻ 最初から再生」は常に最初から。新しい問題・判定でも位置は最初に戻る。判定すると繰り返しは止まる。
- 出題セット（`#charsetSel` の順番どおり）:
  英字 / 数字 / 英数字混在 / 単語 / Q符号・略符号 / コールサイン (6文字) / コールサイン (移動運用・8文字) / コールサイン (混合) /
  コンテストナンバー (ALL JA/6m AND DOWN) / (FD) / (オール岡山) / (全市全郡)
- 文字数（単語数）欄は、変更できないセット（コールサイン系・コンテストナンバー系）では**欄ごと非表示**にし、説明文だけ出す。
- コンテストナンバー系は「5NN xy」で出題し、**5NN の後ろ（xy）だけを答えれば正解**。
- 「**音を可視化**」トグル（`#visBtn`、aria-pressed。**デフォルトはオフ**、状態は `settings.vis` に保存）。オンにすると操作ボタンの下にキャンバスを出し、鳴った分だけ長点・短点を細い帯（太さ6px・角丸）で描く（縮尺は固定で、短点＝帯の太さの丸い点・長点＝その3倍。問題の長さや速度に関係なく見た目は同じで、流れる速さの方が変わる。はみ出す分は再生位置に合わせて左へ送る。先の符号は見せない）。左端のランプは音が鳴っている間だけ点灯。描画は `drawVisualizer`、再生の時刻は `playOnce` が `player.vis` に記録し、通常練習の再生分（`visToken`）だけを描く。
- Q符号・略符号セット: CQ DE QTH QRZ QSL QRM QRN QRP QRO QSY QRT RST TNX 73 88 R（AR/SK/BT/KN は出さない）。

### スコアアタック（タブ「スコアアタック」）
- 正解は入力が一致した瞬間に自動判定→自動で次へ。Enter で誤答表示。問題は正解まで繰り返し再生。
- 一時停止は**音だけ止めて時間は進む**。一時停止中に正解したら自動で再生再開。ミュート、最初から再生、途中終了（ページ内の確認ダイアログ。途中終了は記録しない）。
- ゲーム中は速度変更不可（音量は可）。開始前に 3-2-1 カウントダウン。
- モード（`#atkMode`）と制限時間:

| モード | 出題 | 時間 | 得点 |
|---|---|---|---|
| ノーマルモード | 段階制（下表） | 2分 | 1問1点 |
| コールサインモード | 実在局の6文字コールサイン | 2分 | 1問1点 |
| コンテストナンバーモード | 選んだコンテストのナンバーのみ | 2分 | 1問1点 |
| コンテストモード | コールサイン→その局のナンバーを交互 | 3分 | **両方正解で1点** |

- ノーマルモード（キー `practice`。旧表記「練習モード」）の段階: 0–2点 英数字1文字 / 3–5点 2文字略符号(TU GM GA GE GN FB OM YL HI UR ES OP WX BK CL NR DR CU)または73・88 / 6–9点 3文字略符号・Q符号(`ABBR3`) / 10–14点 コールサイン6文字 / 15点〜 6文字または8文字(移動運用)。
- コンテストナンバー/コンテストモードはコンテスト（`#atkContest`: ALL JA/6m AND DOWN, FD, オール岡山, 全市全郡）を選べる。
- コンテストモード説明文の例（`CONTESTS[*].cex/ccall`）: ALL JA `JR4ZUZ → 31M` / FD `JR4ZUZ/4 → 31M` / オール岡山 `JR4ZUZ → 3110` / 全市全郡 `JR4ZUZ → 3110M`。
- ハイスコア: モード別（コンテスト系はコンテスト別も）に上位10件。キー例 `practice`, `callsign`, `number:fd`, `contest:acag`。モードキー無しの旧記録は `practice` 扱い。名前（任意）、速度、日時を保存。1件削除・表示中モードの全削除（確認あり）。0点は記録しない。

## コールサインの規則

- 元データ: `CALLS_J`（J[A-S]+数字+英字3, 1901局）と `CALLS_7`（7[K-N]+数字+英字3, 64局）。**J 約9割 / 7 約1割**で選ぶ（`callsign6`）。8で始まるものは出さない。
- 移動運用8文字は `callsign6() + '/' + 0〜9`。

## コンテストナンバーの規則（規約確認済み）

- **ALL JA / 6m AND DOWN**: 都府県・地域等ナンバー 02〜47、北海道は地域 101〜114（01 は使わない）、小笠原 48 ＋ 電力 H/M/L/P。
- **FD（1.9〜1200MHz）**: 同じナンバー ＋ 電力 **M/L/P のみ**（H なし）。
- **オール岡山**: 県外局は都府県・地域等ナンバー（31 を除く）、県内局は岡山県の市区郡ナンバー（岡山市は区番号 310101〜310104）。電力記号なし。
- **全市全郡**: JARL の市・郡・区ナンバー（現行分 1,345件: 市795・区171・郡379）＋ 電力 H/M/L/P。政令指定都市は区番号のみ（市番号は除外）。東京23区は市扱い（100101〜）。
- コンテストモード（`contestPair`）は**実在の「コールサイン:ナンバー」の組**から出題する（公開ログ由来。電力記号はログに無いので出題時にランダムで付ける）:
  - `PAIRS_ALLJA`（ALL JA 2025・2026 と 6m AND DOWN 2025 をまとめたもの）/ `PAIRS_FD`（FD 2025）/ `PAIRS_ACAG`（全市全郡 2025）。書式は `CALL:NUM` をスペース区切り。
  - オール岡山のログは無いので、県内（岡山の市区郡ナンバー `31…`、`/4` の移動局を含む）を `PAIRS_ACAG` から7割、県外（`31` 以外）を `PAIRS_ALLJA` から3割で選ぶ。
  - `tools/build_data.py` がログを取り込むときの検査: コールサインの形式（8J などの記念局や `/QRP` は捨てる）、ナンバーの形式（コンテストごとに有効な番号か）、エリアの一致（`/n` があればその数字、無ければコールのエリア数字。**7K〜7N は数字に関係なく関東**）。現状の除外は ALL JA 43件・FD 5件・全市全郡 3件で、エリア不一致はゼロ。
  - エリア→都府県コード: 1関東(10〜17, 48小笠原) 2東海 3近畿 4中国 5四国 6九州沖縄 7東北 8北海道 9北陸 0信越。
- コンテストナンバーモード（ナンバーのみ）と通常練習のコンテストナンバー系は、従来どおり有効なナンバー全体からランダム（`contestExchange`）。

## 未対応・今後の候補

- オール岡山の実ログは未入手（入手したら `data/logs/` に置き、`build_data.py` の `LOGS` に追加する）。
- 新しい年のログは `data/logs/` に追加し、`LOGS` に名前を足して `python3 tools/build_data.py` を実行する。
- 早見表は A–Z, 0–9, ?, / のみ。

## 出典

- JARL: ALL JA / 6m AND DOWN / フィールドデー / 全市全郡 各コンテスト規約、市郡区番号リスト
- JARL岡山県支部: オール岡山コンテスト規約
- Super Check Partial (MASTER.SCP)
- ALL JA（2025・2026）/ 6m AND DOWN・フィールドデー・全市全郡（2025）の公開ログ（コールサインとナンバー）
